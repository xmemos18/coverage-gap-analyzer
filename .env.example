# Coverage Gap Analyzer - Environment Variables Example
# Copy this file to .env.local and fill in your values

# Database Configuration (Required)
# Supabase PostgreSQL connection string
# Get this from: Supabase Dashboard → Settings → Database → Connection string (Transaction mode)
# Format: postgres://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=

# Optional: Direct connection for migrations (Pooler mode for app, direct for migrations)
# Get this from: Supabase Dashboard → Settings → Database → Connection string (Direct)
# Format: postgresql://postgres.[project-ref]:[password]@db.[project-ref].supabase.co:5432/postgres
DATABASE_URL_DIRECT=

# Healthcare.gov Marketplace API (Optional)
# Request free API key at: https://developer.cms.gov/marketplace-api/key-request.html
# Leave empty to use estimated costs instead of real plan data
# NOTE: This is server-side only (no NEXT_PUBLIC_ prefix) for security
HEALTHCARE_GOV_API_KEY=

# Medicare.gov API Configuration (Optional)
# Medicare Plan Finder API access (free, but requires registration)
# Register at: https://data.medicare.gov/register
MEDICARE_GOV_API_KEY=

# Cache Configuration (Optional - uses Upstash Redis free tier)
# Get from: https://upstash.com → Create Redis database → REST API
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Error Tracking (Optional - uses Sentry free tier)
# Get from: https://sentry.io → Create project → Client Keys (DSN)
# Steps: 1) Sign up at sentry.io 2) Create a Next.js project 3) Copy DSN
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
# Generate auth token at: https://sentry.io/settings/auth-tokens/
SENTRY_AUTH_TOKEN=

# Site URL (for production)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# CORS Configuration (Optional - for API access from other domains)
# Comma-separated list of allowed origins for API requests
# Leave empty to use defaults (localhost in dev, main domain in production)
# Example: https://app.example.com,https://mobile.example.com
ALLOWED_ORIGINS=

# =============================================================================
# AUTHENTICATION & SECURITY (Required for Production)
# =============================================================================

# JWT Secret for secure session tokens (REQUIRED IN PRODUCTION)
# Generate a strong secret: openssl rand -base64 32
# SECURITY: Must be at least 32 characters, random, and kept secret
# IMPORTANT: If you change this, all existing sessions will be invalidated
JWT_SECRET=

# Site Password for basic authentication (REQUIRED IN PRODUCTION)
# SECURITY: Use a strong, unique password (not the default!)
# NOTE: SITE_PASSWORD is server-side only (secure)
SITE_PASSWORD=

# DEPRECATED: Client-side password (INSECURE - do not use in production!)
# This exposes the password in the browser's JavaScript bundle
# Only use for local development if needed
# NEXT_PUBLIC_SITE_PASSWORD=

# Enable authentication middleware (set to 'true' to require login)
REQUIRE_AUTH=false

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit .env.local with real credentials to version control
# 2. Rotate credentials regularly, especially after any suspected breach
# 3. Use strong, unique values for JWT_SECRET and SITE_PASSWORD
# 4. In production, ensure REQUIRE_AUTH=true and proper secrets are set
# 5. Rate limiting uses in-memory storage by default - configure Redis for
#    production deployments with multiple server instances

# Environment
NODE_ENV=development
